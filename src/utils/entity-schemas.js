/**
 * Definitions for various database entities and their schemas.
 * Each schema contains configuration for select fields, authentication requirements, and default query parameters.
 */
const entitySchemas = [
    {
        table: 'profiles'
    }
]

export default entitySchemas